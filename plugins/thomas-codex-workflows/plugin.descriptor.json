{
  "schemaVersion": 1,
  "name": "thomas-codex-workflows",
  "displayName": "Thomas Codex Workflows",
  "summary": "Personal Codex hooks for repo safety, Warp CWD updates, PR creation reminders, and worktree lifecycle support.",
  "purpose": "Use this plugin to make ordinary Codex sessions safer through hooks that run around shell commands, post-tool Warp updates, and worktree create/remove events.",
  "whenToUse": [
    "Guard risky shell commands and remind about review gates before PR creation.",
    "Prepare and clean up worktree metadata during worktree lifecycle events."
  ],
  "surfaces": {
    "skills": [],
    "mcpServers": [],
    "hooks": [
      "PostToolUse",
      "PreToolUse",
      "WorktreeCreate",
      "WorktreeRemove"
    ]
  },
  "safety": {
    "publicSafe": true,
    "authModel": "Uses local command state such as git and gh without publishing credentials.",
    "writeModel": "Hooks may block risky commands or update local worktree metadata, but do not publish remote changes by themselves.",
    "excludedState": [
      "GitHub auth tokens",
      "project memories",
      "logs",
      "session state"
    ]
  },
  "installNotes": [
    "Hooks rely on the Codex compatibility variable for plugin root resolution.",
    "This plugin is machine-local behavior, not a source of secrets or project memory."
  ]
}
